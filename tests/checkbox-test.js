/* global QUnit, assert */

QUnit.test("checkbox with hidden field", function(assert) {
  assert.ok( 1 === 1, "Passed!" );
});

QUnit.test("checkbox without hidden field", function(assert) {
  assert.ok( 1 === 1, "Passed!" );
});
